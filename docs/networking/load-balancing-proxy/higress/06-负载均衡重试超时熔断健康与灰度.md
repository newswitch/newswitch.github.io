---
title: "负载均衡、重试、超时、熔断、健康与流量灰度"
sidebar_label: "06. 负载均衡、重试、超时、熔断、健康与流量灰度"
sidebar_position: 6
description: "设计网关可靠流量治理预算，避免重试风暴和错误灰度。"
tags: [Higress, Load Balancing, Retry, Circuit Breaker]
---

# 负载均衡、重试、超时、熔断、健康与流量灰度

## 1. 预算 {/* #预算 */}

```text
client deadline
> gateway route timeout
> per-try timeout
> upstream internal timeout
```

重试次数 × per-try 必须落在总预算内，且留网络/排队时间。网关、Sidecar、SDK、应用不能各自重试多次。

## 2. LB/健康 {/* #lb健康 */}

Round-robin、least request、ring hash 等按连接/请求/Key交换。主动健康发现不可用，Outlier Detection 按连续失败/成功率临时驱逐；过敏参数会在短故障中驱逐所有 Endpoint。

## 3. Circuit Breaker {/* #circuit-breaker */}

限制连接、Pending、请求和重试，保护 Gateway/上游；触发返回 503 等并应区分 no healthy upstream。它不替代业务降级和全局限流。

## 4. 灰度 {/* #灰度 */}

按 Header/Cookie/用户 hash/权重路由，标识必须可信且稳定。灰度同时控制配置、Endpoint、插件和镜像版本；比较同窗口错误/P99/业务指标并自动回滚。

## 5. 幂等 {/* #幂等 */}

只有幂等且未输出响应的请求适合自动重试；LLM Streaming 已输出 token 后不能换后端无缝重试。携带 request/attempt ID。

## 6. 治理策略实验顺序 {/* #治理策略实验顺序 */}

先建立无治理基线，再依次加入超时、重试、熔断和灰度，每次只改变一项。后端分别注入连接失败、慢首包、处理中断和 5xx，记录网关 P99、上游请求次数、重试放大、熔断状态和恢复时间。

```text
客户端总 deadline > 网关处理 + 每次上游 timeout × 最大尝试 + 网络余量
```

重试只用于明确幂等的请求，并受总 deadline 和 retry budget 限制；熔断保护下游但可能造成快速失败；健康检查必须从真实网关网络执行。灰度键缺失、权重归一、会话粘滞和版本回退都要有测试。

Higress 插件/策略字段随 2.x 演进，示例上线前用当前 CRD/Console 文档校对并检查资源 status。策略效果必须由访问日志和上游计数证明，而不是只确认配置对象存在。

## 7. 验收题 {/* #验收题 */}

- 多层重试如何放大流量？
- Outlier 参数过激会怎样？
- Circuit Breaker 与 Rate Limit 有何不同？
- 流式响应何时不能重试？

## 8. 参考资料 {/* #参考资料 */}

- [Higress traffic governance](https://higress.cn/en/docs/latest/user/traffic-governance/)
